# tools

Repository tooling that is not part of any shipped artifact.

- `verify_project_identity.py` — asserts that the build files still match the
  frozen `ProjectIdentityBuildContractV1`.

The governance validator and its self-test live in `scripts/`.
