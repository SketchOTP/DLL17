# migrations

Schema and persisted-state migrations. Empty in R000: nothing is persisted, so
there is nothing to migrate. The first entry is created when a canonical state
schema is first written to storage.
